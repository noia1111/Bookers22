<div class="container">
  <div class="row">
    <div class="col-md-3">
            <div>
        <h2>User info</h2>
        <%= image_tag current_user.get_profile_image(100,100) %>
        <table>
          <tbody>
            <tr>
              <th>name</th>
              <td><%= current_user.name %></td>
            </tr>
            <tr>
              <th>introduction</th>
              <td><%= current_user.introduction %></td>
            </tr>
          </tbody>
        </table>
        <%= link_to edit_user_path(current_user.id), class: "btn btn-outline-secondary" do %>
          　　　　　　<i class="fas fa-user-cog"></i>　　　　　　
        <% end %>  
      </div>
      
      <%= render 'books/list' %>

    </div>
    
    
    
    
    
    

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table>
        <thead>
          <tr>
            <th>images</th>
            <th>name</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
        <% @users.each do |user1| %>
          <tr>
            <td><%= image_tag user1.get_profile_image(100,100) %></td>
            <td><%= user1.name %></td>
            <td><%= link_to "Show", user_path(user1.id) %></td>
        <% end %>
          </tr>
        </tbody>
      
      </table>
    </div>
  </div>  <!-- この行を追加 -->
</div> 