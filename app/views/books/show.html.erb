    <%= flash[:notice] %>

<div class="container">
  <div class="row">
    <div class="col-md-3">
            <div>
        <h2>User info</h2>
        <%= image_tag @book.user.get_profile_image(100,100) %>
        <table>
          <tbody>
            <tr>
              <th>name</th>
              <td><%= @book.user.name %></td>
            </tr>
            <tr>
              <th>introduction</th>
              <td><%= @book.user.introduction %></td>
            </tr>
          </tbody>
        </table>

            <%= link_to edit_user_path(@book.user), class: "btn btn-outline-secondary" do %>
      　　　　　　<i class="fas fa-user-cog"></i>　　　　　
      　　　　　　<% end %>
            </div>
      
    
      <div class="order-md-last">
        <h1>New book</h1>
        <%= form_with model: @book2 do |f| %>
          <h4>Title</h4>
          <%= f.text_field :title %>
          <h4>Opinion</h4>
          <%= f.text_area :body %>
          <%= f.submit 'Create Book', class:"btn btn-success" %>
        <% end %>
      </div>

    </div>
    
    
    
    
    
    

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table>
        <tbody>
          <tr>
            <td>
            <%= link_to user_path(@book.user_id), class: "text-decoration-none" do %>
              <%= image_tag @book.user.get_profile_image(100,100) %>
              <br><%= @user.name %>
            <% end %>
              </td>
        <td><%= link_to @book.title, book_path(@book.id) %></td>
        <td><%= @book.body %></td>
        <% if current_user == @user %>
          <td><%= link_to "Edit", edit_book_path(@book.id), class:"btn btn-sm btn-success" %></td>
          <td><%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？", class:"btn btn-sm btn-danger" %></td>
        <% end %>
          </tr>
        </tbody>
      
      </table>
    </div>
  </div>  <!-- この行を追加 -->
</div> 